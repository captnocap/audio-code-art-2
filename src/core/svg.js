// Stub for SVG exporter
export const svgExporter = {
    scale: (x, y, w, h) => ({ x, y }),
    line: () => '',
    circle: () => '',
    rgbToHex: () => '#ffffff',
    createDocument: () => '',
    width: 1920
}
